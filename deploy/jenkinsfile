pipeline {
    agent any

  stages {

     stage("Initial cleanup") {
          steps {
            dir("${WORKSPACE}") {
              deleteDir()
            }
          }
        }

    stage('Checkout SCM') {
      steps {
            git branch: 'main', url: 'https://github.com/jaymineh/Todo.git'
      }
    }

    stage('Prepare Dependencies') {
      steps {
             su sh 'mv .env.sample .env'
             su sh 'composer install'
             su sh 'php artisan migrate'
             su sh 'php artisan db:seed'
             su sh 'php artisan key:generate'
      }
    }
  }
}