pipeline {
    agent any

  stages {

     stage("Initial cleanup") {
          steps {
            dir("${WORKSPACE}") {
              deleteDir()
            }
          }
        }

    stage('Checkout SCM') {
      steps {
            git branch: 'main', url: 'https://github.com/jaymineh/Todo.git'
      }
    }

    stage('Prepare Dependencies') {
      steps {
             sh su 'mv .env.sample .env'
             sh su 'composer install'
             sh su 'php artisan migrate'
             sh su 'php artisan db:seed'
             sh su 'php artisan key:generate'
      }
    }
  }
}